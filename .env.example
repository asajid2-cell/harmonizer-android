# Harmonizer Android has no runtime .env file.
# The backend URL is a compile-time BuildConfig field set in app/build.gradle.kts:
#
#   buildConfigField("String", "BASE_URL", '"https://harmonizerlabs.cc/"')
#
# To point at a different backend, change that line and rebuild.
# Never commit local.properties — it contains your local Android SDK path.
